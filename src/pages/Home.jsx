import Navbar from '../components/Navbar';
import StartPage from './StartPage';

const Home = () => {
    return (
        <div>
            <Navbar />
            <StartPage />
        </div>
    );
};

export default Home;